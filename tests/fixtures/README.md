# Synthetic ingestion fixtures

This directory contains only invented resume content. The DOCX and PDF equivalents are assembled by
`test_phase_f_acceptance.py` so the repository never carries a real CV or contact details.
